#include <stdio.h>

/**
 * main - function to print its name
 * @argc: agrc parameter
 * @agrv: an array of command listed
 * Return: 0 for success
 *
 */
int main(int agrc, char *agrv[] __attribute__((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}

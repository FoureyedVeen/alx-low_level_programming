#include "main.h"

/**
 * add - a function that adds two integer and return the result
 * @i: first number input
 * @j: second number input
 *
 * Return: sum of both integers
 */
int add(int i, int j)
{
	return (i + j);
}
